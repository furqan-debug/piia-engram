# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Engram 用户数据（绝不提交）
.engram/
.piia/
*.env
.env.*
engram_backup_*.json
identity_card.md
reader_cache/

# Secrets / credentials（防止意外提交）
*.pem
*.key
*.p12
*.pfx
credentials*
secrets*

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Generated temp files
test_output.txt
test_stdout.txt
test_out.txt
my_ai_profile.html
.claude/
.mypy_cache/
.ruff_cache/

# Benchmark & evaluation outputs (results stay local, code & REPORT.md go in)
experiments/**/results_*.json
experiments/**/results_*.jsonl
experiments/**/raw_log_*.jsonl
experiments/**/scores.json
experiments/**/facts.json
experiments/**/chatgpt_scores.json

# Coverage artifacts
.coverage
htmlcov/
docs/coverage/
worker/.wrangler/

# Root server.json — stale copy left by old mcp-publisher workflow.
# Canonical source is .mcp/server.json; publish runs `cd .mcp && mcp-publisher publish`.
/server.json

# Internal-only doc drafts (not for publishing)
docs/_drafts/

# Competitive intelligence & internal strategy (never publish)
experiments/competitive_landscape*.md
experiments/beta_recruitment*.md
docs/messaging.md
docs/vision.md

# Strategic / weakness-exposing docs (v3.30 second-pass sanitization)
# These either: (a) expose self-assessed weak spots to competitors,
# (b) reveal design rationale that's our competitive edge, or
# (c) hand competitors a roadmap they can copy / counter.
docs/design/
docs/telemetry_roadmap.md
docs/playbook-auto-extraction-design.md
docs/milestone_review_*.md
docs/milestone_review_*.html
experiments/evaluations/v3.16.0/cross_ai/
# Backup directory for sanitization (kept outside the repo by path,
# but defensive in case anyone clones it sideways into the tree)
**/engram-private-strategy/
